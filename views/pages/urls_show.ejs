<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head') %>
</head>
<body>
  <%- include('../partials/header') %>
  <div class="card text-center">
    <div class="card-header"></div>
    <div class="card-body">
      <h5 class="card-title">BabyURL for: <%= longURL %></h5>
      <p class="card-text">Short URL: <a href="/u/<%= shortURL %>"><%= shortURL %></a></p>
      <p class="card-text">
        <form class="form-inline" action="/urls/<%= shortURL %>/update" method="POST">
          <div class="form-group mb-2">
            <label for="longURL">Update this URL:</label>
            <input class="form-control" type="text" name="longURL" placeholder="http://" style="width: 300px; margin: 1em">
            <button type="submit" class="btn btn-primary">Submit</button>
          </div>
        </form>
      </p>  
    </div>
  </div>
</body>

<%- include('../partials/footer') %>
</html>
